# API
PORT=3000
HOST=0.0.0.0

# Rate limit
RATE_LIMIT_MAX=15
RATE_LIMIT_WINDOW_MS=60000

# Provider strategy (reserved for future external provider integration)
TEXT_PROVIDER=fake
IMAGE_PROVIDER=fake
